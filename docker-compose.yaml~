version: "3.8"
services:
    frontend:
          build: ./chat-Frontend
          container_name: pensive_rosalind
          ports:
           - '3000:3000'
          command: npm run dev
    backend:
          build: ./backend
          container_name: boring_cerf
          ports:
           - '4000:4000'
          stdin_open: true
          tty: true
          command: npm run dev
